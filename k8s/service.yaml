apiVersion: v1
kind: Service
metadata:
  name: platform-api
  namespace: platform
  labels:
    app: platform-api
spec:
  type: NodePort
  selector:
    app: platform-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
    nodePort: 30080
    name: http
